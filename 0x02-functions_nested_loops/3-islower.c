#include "main.h"
/**
 * _islower - Shows 1 if the input is a lower character, another case shows 0
 *
 * @c: The character in ASCII code
 *
 * Return: 1 for lowercase character, 0 for the rest
 */
int _islower(int c)
{
	if (c > = 97 && c < = 122)
	{	
		return(i);
	}
	else
	{
		return(0);
	}
	_putchar('\n')
}
